Below is an example of me using it to search within the test.txt file for the word "not"
<img width="2352" height="213" alt="Screenshot 2026-09-14 221849" src="https://github.com/user-attachments/assets/c7696767-cb7d-413a-b0d2-5a995238a20d" />
```c
./mini-grep.exe {"string"} {filepath}
```

```terminal
$ gcc main.c -o mini-grep.exe && ./mini-grep.exe "not" test.txt
Alt text should communicate what an image means, not how it looks.
Some images, such as charts or graphs, require long descriptions, but not all document types allow that.
When columns are not created correctly, screen readers may run lines together, reading the first line of the first column, then the first line of the second column, then the second line of the first column, and so on.
```
